#include <stdio.h>
/**
 *_isuppert(int c) - checks for uppercase
 *Returns: 0 if lowercase else 1
 */
int _isupper(int c)
{
	if ((c >= 'A')) && ((c <= 'Z'));
	{
		return (1);
	}
	else
		return (0);
}
